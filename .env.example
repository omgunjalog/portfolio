# Contact form email delivery (optional).
# Without these, the form gracefully falls back to opening the visitor's
# email client prefilled — nothing is ever silently dropped.
#
# To enable server-side delivery, create a free account at resend.com,
# add your API key here, and (optionally) a verified from-address.
RESEND_API_KEY=
CONTACT_FROM="Om Gunjal <onboarding@resend.dev>"
